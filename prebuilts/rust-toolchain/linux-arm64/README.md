# Linux ARM64 Android Rust toolchain

This directory will contain the generated native Linux AArch64 Rust toolchain compatible with the pinned Android/Axion branch.

Build it with `scripts/build-rust.sh`, verify it with `scripts/check-prebuilt.sh`, then run `scripts/track-large-files.sh` before committing.
