# Linux ARM64 JDK 21

This directory will contain the generated AOSP-compatible native Linux AArch64 JDK 21 tree.

Build it with `scripts/build-jdk21-arm64.sh`, verify it with `scripts/check-prebuilt.sh`, then run `scripts/track-large-files.sh` before committing.
