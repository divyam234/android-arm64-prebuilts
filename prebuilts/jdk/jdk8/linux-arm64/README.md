# JDK 8 Linux ARM64

Native Linux AArch64 JDK 8 prebuilt used as an Android build-host JDK where older AOSP tooling still expects JDK 8.

This directory is populated by `scripts/import-jdk8-arm64.sh` from a pinned AArch64 JDK 8 distribution. Large files are tracked with Git LFS.
